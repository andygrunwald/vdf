"Broken Comment Sample v.1"
{
  // This is a valid comment. `uri` and `timeout` will be parsed as expected
 "uri" "http://127.0.0.1:3456"
 "timeout" "8.0"
 / This is a broken/invalid comment. Parsing will stop here and `buffer` ignored
 "buffer"  "0.3"
}